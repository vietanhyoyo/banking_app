import 'package:flutter/material.dart';

class BankingHistoryPage extends StatelessWidget {
  const BankingHistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("History"));
  }
}
